# Copy this file to .streamlit/secrets.toml and fill in your values.
# On Streamlit Cloud, paste these key-value pairs into Settings → Secrets.

# Optional: Fernet key for encrypted telemetry bridge
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TELEMETRY_FERNET_KEY = ""

# Optional: Copernicus Open Access Hub credentials (leave blank for simulation mode)
SENTINEL_USER     = ""
SENTINEL_PASSWORD = ""
