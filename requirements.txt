# ── Aether Intelligence — Streamlit Cloud requirements ────────────────────────
# Minimum set required for dashboard.py + shared/intelligence.py.
# All heavy orbital-gateway deps (ultralytics, onnxruntime, sentinelsat) are
# excluded here — they run locally only and are not needed for the Cloud UI.

streamlit>=1.35.0
pandas>=2.2.0
cryptography>=42.0.0
yfinance>=0.2.40
python-dotenv>=1.0.0
